## Description

<!-- What does this PR do and why? -->

## Checklist

- [ ] `tofu validate` passes locally (`cd tofu/envs/lab && tofu init -backend=false && tofu validate`)
- [ ] Changelog updated if this is a user-facing change
- [ ] No secrets, IPs, or account IDs committed
- [ ] ADR added under `docs/decisions/` if this introduces a non-trivial decision
- [ ] Documentation updated if behavior or usage changed
